## Environment Setup

This project uses `python-dotenv` for clean environment variable management. 

1. Create a `.env` file in the root of the `code/` directory (or the project root).
2. Add your Google Gemini API key:
   ```env
   GEMINI_API_KEY=your_api_key_here